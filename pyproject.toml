[tool.poetry]
name = "elapi"
version = "0.25"
description = "elabftw API client for the FIRE division at Universität Heidelberg"
authors = [
    "Mahadi Xion <mahadi.xion@urz.uni-heidelberg.de>",
    "Alexander Haller <alexander.haller@urz.uni-heidelberg.de>"
]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
elabapi-python = "^0.3.0"
dynaconf = "^3.2.0"
httpx = "^0.24.1"
httpx-auth = "^0.17.0"
typer = { extras = ["all"], version = "^0.9.0" }
pyyaml = "^6.0.1"

[tool.poetry.group.dev.dependencies]
ptpython = "^3.0.23"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
